version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
  build:
    commands:
      - npx run build

artifacts:
  base-directory: ${CODEBUILD_SRC_DIR}
  enable-symlinks: yes
  files:
    - '**/*'

cache:
  paths:
    - './node_modules/**/*'